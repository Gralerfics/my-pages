name: Sync notes to Pages

on:
  repository_dispatch:
    types: ["sync-notes"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/build-and-deploy-pages.yml
    secrets: inherit
